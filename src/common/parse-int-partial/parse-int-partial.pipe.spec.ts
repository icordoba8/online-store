import { ParseIntPartialPipe } from './parse-int-partial.pipe';

describe('ParseIntPartialPipe', () => {
  it('should be defined', () => {
    expect(new ParseIntPartialPipe()).toBeDefined();
  });
});
